# Notes

Maintained automatically. Entries are appended during work when issues are found.
Run all tests: `bash scripts/test.sh` (unit only) · `bash scripts/test.sh --e2e` (+ E2E, requires docker compose up -d)

---
